# Bilibili MCP Server 配置
#
# 使用方式：
#   1. 至少设置 SESSDATA + BILI_JCT + DEDE_USER_ID
#   2. 如需 Cookie 自动刷新，额外设置 REFRESH_TOKEN
#   3. 如需热搜等功能稳定运行，建议配全 Cookie（见最下方）
#
# ── 必填（从浏览器获取） ────────────────────────────────────
# 打开 https://www.bilibili.com → F12 → Application → Cookies
# 复制以下三个值：

BILIBILI_SESSDATA=your_sessdata_here
BILIBILI_BILI_JCT=your_bili_jct_here
BILIBILI_DEDE_USER_ID=your_uid_here

# ── Cookie 自动刷新（可选） ─────────────────────────────────
# 如需长期运行不中断，请设置以下值：
# 获取方式：在 bilibili.com 按 F12 → Console → 输入:
#   console.log(localStorage.getItem('ac_time_value'))
# 把输出的值粘贴到下面：

# BILIBILI_REFRESH_TOKEN=xxx

# Cookie 文件路径（可选，优先级低于环境变量）
# 如配合 Cookie 自动刷新使用，新值会自动写入此文件
# BILIBILI_COOKIE_FILE=/path/to/cookie.json

# 是否启用自动刷新（默认 true）
# BILIBILI_AUTO_REFRESH=true

# 检测回复的状态文件路径（可选）
# 用于持久化记录上次检查新回复的时间戳
# 不设置则不会持久化状态，每次重启都会从头检测
# BILIBILI_DETECT_FILE=/path/to/bilibili-detect.json

# ── 完整 Cookie 配置（可选，建议） ─────────────────────────
# 部分接口（如热搜）需要额外的 Cookie 字段才能正常工作。
# 如果遇到 -412 错误，建议将完整 Cookie 设为一行：
# BILIBILI_FULL_COOKIE="buvid3=xxx; buvid4=xxx; b_nut=xxx; _uuid=xxx; ..."
